name: crewmeister_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  get_it: 8.0.3

  # Local pacakges will be moved to git
  crewmeister_core:
    path: ./package/crewmeister_core
  absence_manager:
    path: ./package/absence_manager

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
